# Keeps pytest's rootdir at tests/ and its cache out of the repository root,
# which holds only R/, python/, tests/, log/, classifications/, local_context/
# and four files. Run the suite from anywhere:
#
#   uv run --project python python -m pytest tests/pytest
#
[pytest]
testpaths = pytest
cache_dir = ../local_context/.pytest_cache
